/*
    ================================================================================
PHONEBOOK • Selenium + TestNG • VOM (Helpers + Value Objects) • ШАБЛОН v1.0 (RU)
================================================================================
Документ описывает эталонную структуру проекта Selenium + TestNG.
Формат работы: выполняй шаги сверху вниз, без возвратов.

БАЗОВЫЕ ПРИНЦИПЫ(читаешь сверху вниз → делаешь шаг → ставишь [x] → идёшь дальше.)
-------------------------------------------------------------------------------

Правило №1: В тестах НЕ должно быть driver.findElement(...) вообще.
Правило №2: Локаторы живут только в Helper-классах.
Правило №3: Models (User/Contact) — только данные, не содержат WebDriver и без UI-логики.

===============================================================================
ШАГ 1 — СТРУКТУРА ПРОЕКТА                                               [  ]
===============================================================================

Создать пакеты:

src/main/java/com/phonebook
    core
    fw
    models
    data

src/test/java/com/phonebook
    tests

Назначение слоёв:
core   — инфраструктура (driver, базовые операции)
fw     — UI-логика (действия пользователя)
models — объекты данных
data   — тестовые значения
tests  — сценарии

Отметка: [ ] → [x]

===============================================================================
ШАГ 2 — DATA СЛОЙ (ЦЕНТРАЛИЗАЦИЯ ЗНАЧЕНИЙ)                             [  ]
===============================================================================

Назначение:
Хранение неизменяемых тестовых значений.

Причина:
- исключение магических строк
- единая точка изменения
- повышение читаемости тестов

-------------------------------------------------------------------------------
2.1 Создать: data/UserData.java                                         [  ]
-------------------------------------------------------------------------------

public class UserData {

    public static final String email = "test@mail.com";
    public static final String password = "Password123!";

}

Принцип:
public  — доступ из тестов
static  — объект создавать не требуется
final   — значение не изменяется

-------------------------------------------------------------------------------
2.2 Создать: data/ContactData.java                                      [  ]
-------------------------------------------------------------------------------

public class ContactData {

    public static final String name = "John";
    public static final String surname = "Smith";
    public static final String phone = "123456789";
    public static final String email = "john@mail.com";
    public static final String address = "Berlin";
    public static final String description = "QA contact";

}

Важно:
Data-классы не содержат методов.

Отметка: [ ] → [x]

===============================================================================
ШАГ 3 — MODELS (VALUE OBJECTS)                                          [  ]
===============================================================================

Назначение:
Передача данных в Helper одним объектом.

Преимущество:
- отсутствие длинных списков параметров
- масштабируемость
- снижение риска перепутать порядок аргументов

-------------------------------------------------------------------------------
3.1 Создать: models/User.java                                           [  ]
-------------------------------------------------------------------------------

Требования:
- private поля
- public getters
- fluent setters (возвращают this)

Пример fluent setter:

public User setEmail(String email) {
    this.email = email;
    return this;
}

Модель не содержит UI-логики.

-------------------------------------------------------------------------------
3.2 Создать: models/Contact.java                                        [  ]
-------------------------------------------------------------------------------

Аналогичная структура:
private поля + getters + fluent setters.

Отметка: [ ] → [x]

===============================================================================
ШАГ 4 — BASEHELPER (ЕДИНАЯ ТОЧКА UI-ДЕЙСТВИЙ)                           [  ]
===============================================================================

Назначение:
Инкапсуляция базовых операций WebDriver.

Преимущество:
Изменения механизма ожиданий или обработки элементов
затрагивают один класс.

-------------------------------------------------------------------------------
Создать: core/BaseHelper.java                                           [  ]
-------------------------------------------------------------------------------

Структура:

protected WebDriver driver;

public BaseHelper(WebDriver driver) {
    this.driver = driver;
}

Минимальный API:

protected void click(By locator);

protected void type(By locator, String text);
    если text == null — действие не выполняется

protected boolean isElementPresent(By locator);

protected boolean isAlertPresent();
    если alert найден → accept() → true
    если нет → false (через try/catch)

BaseHelper не содержит бизнес-логики.

Отметка: [ ] → [x]

===============================================================================
ШАГ 5 — HELPER-СЛОЙ (UI-ЛОГИКА)                                         [  ]
===============================================================================

Назначение:
Инкапсуляция локаторов и порядка действий.

Тест формулирует ЧТО сделать.
Helper определяет КАК это сделать.

-------------------------------------------------------------------------------
5.1 UserHelper                                                          [  ]
-------------------------------------------------------------------------------

Методы:
- clickOnLoginLink()
- fillLoginRegisterForm(User user)
- clickOnLoginButton()
- clickOnRegistrationButton()
- isSignOutButtonPresent()
- clickOnSignOutButton()

-------------------------------------------------------------------------------
5.2 ContactHelper                                                       [  ]
-------------------------------------------------------------------------------

Методы:
- clickOnAddLink()
- fillContactForm(Contact contact)
- clickOnSaveButton()
- sizeOfContacts()
- removeContact()

sizeOfContacts() возвращает int
для проверки before/after.

-------------------------------------------------------------------------------
5.3 HomePageHelper                                                      [  ]
-------------------------------------------------------------------------------

Метод:
- isHomeComponentPresent()

Отметка: [ ] → [x]

===============================================================================
ШАГ 6 — APPLICATION MANAGER                                             [  ]
===============================================================================

Назначение:
Централизованное управление driver и helper-ами.

-------------------------------------------------------------------------------
Создать: core/ApplicationManager.java                                   [  ]
-------------------------------------------------------------------------------

Содержит:
- WebDriver driver
- UserHelper user
- ContactHelper contact
- HomePageHelper homePage

Методы:
- init()
- stop()
- getUser()
- getContact()
- getHomePage()

Порядок init():
1) driver setup
2) driver = new ChromeDriver()
3) driver.get(BASE_URL)
4) инициализация helper-ов

Отметка: [ ] → [x]

===============================================================================
ШАГ 7 — TESTBASE                                                        [  ]
===============================================================================

Назначение:
Общая инициализация для всех тестов.

-------------------------------------------------------------------------------
Создать: tests/TestBase.java                                            [  ]
-------------------------------------------------------------------------------

protected ApplicationManager app;

@BeforeMethod
    app.init();

@AfterMethod
    app.stop();

Все тесты наследуются от TestBase.

Отметка: [ ] → [x]

===============================================================================
ШАГ 8 — ТЕСТЫ                                                           [  ]
===============================================================================

Тест содержит только:
- вызовы методов helper-ов
- assert

Пример:

app.getUser().clickOnLoginLink();
app.getUser().fillLoginRegisterForm(
    new User()
        .setEmail(UserData.email)
        .setPassword(UserData.password)
);
app.getUser().clickOnLoginButton();

Assert.assertTrue(
    app.getUser().isSignOutButtonPresent()
);

===============================================================================
КОНЕЦ ДОКУМЕНТА
===============================================================================
 */